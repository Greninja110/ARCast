<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abhijeetsahoo.arcast">

    <!-- Basic permissions only -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- For logging crash info -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:name=".ARCastApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ARCast">

        <!-- Diagnostic Activity as launcher -->
        <activity
            android:name=".DiagnosticActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Minimal Activity -->
        <activity
            android:name=".MinimalActivity"
            android:exported="false" />

        <!-- Debug Activity - no longer launcher -->
        <activity
            android:name=".DebugActivity"
            android:exported="false" />

        <!-- MainActivity - not launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>
</manifest>